# 目視確認および結果整理レポート (2026-05-20)

実データの処理結果を精査し、パイプラインの正常動作を確認しました。

## 1. 実施内容
- **不具合修正**: `src/main.py` で発生していた `KeyError: 'yaml'` を修正し、無限リトライループを解消しました。
- **環境クリーンアップ**: 失敗時に生成された大量の不要な画像を削除し、コンテナを再ビルド・再起動しました。
- **一括処理**: 以下の5つのテスト用実データ（複雑なレイアウト、方眼紙、画像埋め込みを含む）を正常に処理しました。

## 2. 処理結果サマリー

| ファイル名 | ステータス | 主要な抽出内容 | 備考 |
| :--- | :--- | :--- | :--- |
| `complex_report.xlsx` | ✅ 成功 | 施工不良箇所、調査日、特記事項 | 複雑な結合セルの論理構造を正確に把握。 |
| `realistic_business_report.xlsx` | ✅ 成功 | 月次売上、現場写真（座標A3, C3）、所見 | 写真の配置座標とテキストの対応が完璧。 |
| `mega_mixed_report.xlsx` | ✅ 成功 | 複数シート、グラフ、結合セル | 大規模なワークブックでも並列処理が安定。 |
| `complex_kami_sample.xlsx` | ✅ 成功 | 方眼紙レイアウト、複雑な表 | 日本特有のレイアウトでも構造化に成功。 |
| `tiny_test.xlsx` | ✅ 成功 | シンプルなテーブル | シンプル抽出モードへの自動切り替えを確認。 |

## 3. 詳細確認（目視確認）

### 3.1 `realistic_business_report.xlsx` (SitePhotos シート)
- **期待値**: A3, C3 に写真があり、A16 に「全体の構造に異常なし...」の所見がある。
- **抽出結果**: `photoSections` として A3, C3 を認識。`observation` として A16 の所見を正確に抽出。
- **判定**: **合格**

### 3.2 `complex_report.xlsx` (報告概要 シート)
- **期待値**: 「施工不良箇所調査報告書」というタイトルと、2026/03/02 の日付。
- **抽出結果**: `documentTitle` および `metaData.surveyDate` が正しく抽出されている。
- **判定**: **合格**

## 4. 改善済みの課題
- **KeyError: 'yaml'**: `src/main.py` がライブラリ仕様変更前のキーを参照していた問題を修正しました。現在は `_raw_data` を使用して安全に出力されます。
- **ディスク圧迫**: リトライによる画像生成の重複を解消しました。

## 5. 次のステップ
- **実運用への投入**: 監視ディレクトリ `data/input` に本番データを投入する準備が整いました。
- **コスト監視**: 1.5 Flash を使用していますが、より低コストなモデル（Ollama 等）への切り替えテストも可能です。
